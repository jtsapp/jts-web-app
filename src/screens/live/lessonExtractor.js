// Урок каталога на занятии показывается САМИМ ФАЙЛОМ во фрейме — как пробный
// урок, — а не разобранным на шаги.
//
// Разбор («экстрактор»: шаги из JSON каталога, отрисованные LessonContent)
// пересобирал урок в карточки заданий, свою навигацию по темам и свою проверку
// ответов, и каждая из трёх расходилась с файлом по-своему: пропадали задания
// на порядок и пары, терялись звук и картинки, счётчик шагов не совпадал со
// стадиями. Решение владельца 18.09.2026: показывать конкретные html-файлы без
// какой-либо экстракции; правильность ответов считает сервер по ключам, вынутым
// из файла при загрузке, а не разбор на клиенте. Тот же выключатель стоит и в
// рабочей области преподавателя (web-admin, LESSON_EXTRACTOR) — стороны обязаны
// открывать один и тот же материал одинаково, иначе преподаватель видит файл,
// а ученик — карточки, и «экран ученика» перестаёт быть экраном ученика.
//
// Флаг, а не удаление: разбор и его тесты остаются живыми. Объект, а не
// const-литерал — тестам разбора нужно поднять флаг и опустить обратно.
export const LESSON_EXTRACTOR = { enabled: false }
