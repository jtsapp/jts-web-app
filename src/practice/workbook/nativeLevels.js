// Уровни, уже переведённые на нативный экран воркбука (?screen=workbook).
// Остальные пока открываются старым оверлеем с iframe
// (src/practice/workbooks/workbooksOverlay.js) — их порт идёт следом.
//
// Список лежит отдельным модулем намеренно: каталог Практики не должен из-за
// одной константы затягивать в свой бандл весь экран воркбука.
export const NATIVE_WORKBOOK_LEVELS = ['a0']
