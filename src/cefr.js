// Шкала CEFR для экрана теста — только представление.
//
// Адаптивный движок (2PL + EAP) отсюда убран: и подбор вопроса, и проверка
// ответа, и оценка θ теперь на бэкенде (POST /adaptive-test/sessions), потому
// что клиенту нельзя отдавать ключ — банк вопросов публичный.

export const LEVELS = ['A1', 'A2', 'B1', 'B2', 'C1', 'C2']

// Позиция бегунка по шкале: θ ∈ [-3, 3] → 3..97 %.
export const pctOf = (t) => Math.max(3, Math.min(97, ((t + 3) / 6) * 100))
