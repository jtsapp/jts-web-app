node_modules
.DS_Store
dist/
.next/
out/
.env
.env*.local
.vercel
.env*

# Python bytecode from the LiveKit agent
__pycache__/
*.pyc

# Machine-local Claude Code settings (absolute paths, not shareable)
.claude/

# books.html — 109 МБ, превышает лимит файла GitHub (100 МБ), поэтому только
# локально. fairytales.html (2.9 МБ) и обложки сказок (covers/) — коммитим,
# чтобы «Практика» с настоящими обложками работала у всех.
public/practice/books.html

# Playwright
/test-results/
/playwright-report/
/blob-report/
/.playwright/

# Локальный venv питон-агента (ставится из agent/requirements.txt)
agent/venv/

# Local staging of extracted lesson media — uploaded to files-api via mc, not committed
public/learning/media/

# Сырьё Shadowing: выгруженные страницы Notion и промежуточный captions.txt.
# В репозиторий едет только результат нарезки (public/shadowing/*.json) и
# описание материалов — держать вторую копию тех же транскриптов ни к чему.
scripts/shadowing-src/*
!scripts/shadowing-src/sources.json
# Локальный venv питон-агента (ставится из agent/requirements.txt)
agent/venv/
